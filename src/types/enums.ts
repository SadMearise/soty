export enum CssSize {
  Sm = "sm",
  Xsm = "xsm",
  Md = "md",
  Xmd = "xmd",
  MdAdaptive = "mdAdaptive",
  LgAdaptive = "lgAdaptive",
}

export enum HTTPMethod {
  Get = "GET",
  Post = "POST",
  Put = "PUT",
  Delete = "DELETE",
}

export enum HTTPStatusCode {
  Unauthorized = 401,
}

export enum Color {
  Green = "green",
  Grey = "grey",
  White = "white",
  Black70 = "black/70",
  Dark400 = "dark400",
}

export enum TracklistType {
  Album = "album",
  Playlist = "playlist",
}

export enum RestrictionsReason {
  Market = "market",
  Product = "product",
  Explicit = "explicit",
}

export enum CopyrightType {
  C = "C",
  P = "P",
}

export enum AlbumType {
  Album = "album",
  Single = "single",
  Compilation = "compilation",
}

export enum ReleaseDatePrecision {
  Year = "year",
  Month = "month",
  Day = "day",
}

export enum Severity {
  Success = "success",
  Info = "info",
  Warning = "warning",
  Error = "error",
}
